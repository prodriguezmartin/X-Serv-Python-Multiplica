#!/usr/bin/python3

for num1 in range(1,11):
	print("Tabla del " + str(num1))
	print("------------------")
	for num2 in range(1,11):
		print(str(num1) + " por " + str(num2) + " es " + str(num1*num2))
